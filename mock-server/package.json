{
  "name": "rush-app-mock-dev-server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "pnpm clean-mock-server && pnpm mock-server-ts-compile",
    "clean-mock-server": "rm -rf dist",
    "mock-server-ts-compile": "npx tsc",
    "start": "pnpm build && node dist/mock-server/runMockServer.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@types/express": "^5.0.6",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24.13.1"
  },
  "packageManager": "pnpm@10.30.1"
}
